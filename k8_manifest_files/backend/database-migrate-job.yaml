apiVersion: batch/v1
kind: Job
metadata:
  name: database-migrate-job-dev
spec:
  template:
    spec:
      containers:
      - name: backend-container-dev
        image: 782831329154.dkr.ecr.us-east-1.amazonaws.com/coding-with-callie-backend:latest
        env:
        - name: DATABASE_HOST
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DATABASE_HOST
        - name: DATABASE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DATABASE_USERNAME
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: DATABASE_PASSWORD
        - name: DATABASE_NAME
          value: "postgres"
        - name: NODE_TLS_REJECT_UNAUTHORIZED
          value: "0"
        - name: ENVIRONMENT
          value: "production"
        command: ["npm", "run", "migration:run"]
      restartPolicy: Never
